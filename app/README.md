# /app

High-level application workspace for Phase 1 MVP. In Phase 1, client surfaces are split into `/mobile` and `/admin`; this folder exists to keep future organization consistent without adding non-MVP scope.


